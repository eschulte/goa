Documentation Directory                                    -*- org -*-

The contents of each file are listed below.

* optimize-args.txt

  This file holds the arguments to the optimize script used for each
  benchmark program.  Specifically the compiler and the flags used for
  each benchmark are held in the table.

  To print the compiler used for blackscholes run the following.
  : grep blackscholes optimize-args.txt|cut -f2

  To print the flags used for raytrace run the following.
  : grep raytrace optimize-args.txt|cut -f3
