#!/bin/bash

if (( $# == 0 ))
then
  printf "usage: $0 <string to search for>";
  exit 0;
fi

for i in `find .`;
do grep $1 $i 2>/dev/null;
  if (( $? == 0 ))
  then 
      printf " --------------- $i --------------- \n\n";
  fi
done
